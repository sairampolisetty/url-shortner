<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>URL Shortener</title>
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            max-width: 800px;
        }
        .table td, .table th {
            vertical-align: middle;
            text-align: center;
        }
        .table .full-url-link {
            word-break: break-all;
        }
    </style>
</head>
<body>

    <div class="container py-5">
        <header class="text-center mb-5">
            <h1 class="display-4">URL Shortener ✂️</h1>
            <p class="lead">Enter a long URL to create a short one.</p>
        </header>

        <main>
            <form action="/shortUrls" method="POST" class="mb-5">
                <div class="input-group input-group-lg">
                    <input required placeholder="Enter your Long URL..." type="url" name="fullUrl" id="fullUrl" class="form-control">
                    <button class="btn btn-primary fw-bold" type="submit">Shrink</button>
                </div>
            </form>

            <div class="table-responsive">
                <table class="table table-striped table-hover table-bordered shadow-sm">
                    <thead class="table-dark">
                        <tr>
                            <th scope="col">Full URL</th>
                            <th scope="col">Short URL</th>
                            <th scope="col">Clicks</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% shortUrls.forEach(shortUrl => { %>
                            <tr>
                                <td><a class="full-url-link" href="<%= shortUrl.full %>" target="_blank"><%= shortUrl.full %></a></td>
                                <td><a href="/<%= shortUrl.short %>" target="_blank"><%= shortUrl.short %></a></td>
                                <td><%= shortUrl.clicks %></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </main>
        
        <footer class="text-center text-muted mt-5">
            <p>&copy; 2025 URL Shortener.</p>
        </footer>
    </div>

</body>
</html>